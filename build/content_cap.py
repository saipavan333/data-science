# -*- coding: utf-8 -*-
import builder as B
IMG = "../assets/img/"
p = []

p.append(B.why(
    "This is where it all comes together. A stakeholder hands you a year of raw, messy orders and "
    "asks a simple question: *where should we focus next year?* Your job is to turn that "
    "spreadsheet into a defensible recommendation. We will walk the full workflow from Lesson 1.1 "
    "&mdash; load, clean, explore, visualize, decide &mdash; using everything from Track 4. Every "
    "number below is computed from real data, and the whole thing runs."))

p.append(B.callout("tip",
    "What you'll produce (and run yourself)",
    "A complete EDA ending in a one-paragraph insight a manager could act on. The runnable "
    "artifacts ship with this lesson: download [the Jupyter notebook](../notebooks/capstone_eda.ipynb) "
    "and [the dataset](../data/ecommerce_orders.csv), open the notebook next to this page, and run "
    "every cell. The charts here were generated by that exact analysis.", "&#10022;"))

# ---- the data ---- #
p.append(B.h2("The dataset", kicker="Step 0 · know your data"))
p.append(B.concept(
    "We have one row per order from a fictional online retailer for the 2025 calendar year. "
    "Before touching it, write down what each column *should* be (Lesson 4.1) &mdash; it tells you "
    "which summaries will be legal later."))
p.append(B.table(
    ["Column", "Meaning", "Variable type"],
    [["order_id", "unique order identifier", "nominal (a label)"],
     ["order_date", "date the order was placed", "date / ordinal"],
     ["customer_id", "which customer", "nominal (a label)"],
     ["region", "North / South / East / West", "nominal"],
     ["category", "product category", "nominal"],
     ["channel", "Web / Mobile / Store", "nominal"],
     ["quantity", "items in the order", "discrete count"],
     ["unit_price", "price per item ($)", "continuous"],
     ["discount", "fraction off (0&ndash;0.2)", "continuous"],
     ["order_value", "what the order was worth ($)", "continuous"],
     ["is_returned", "was it returned?", "categorical (boolean)"]],
    caption="Knowing each column's type up front prevents nonsense later, like averaging a "
            "customer_id."))

# ---- step 1 load ---- #
p.append(B.h2("Load and inspect", kicker="Step 1"))
p.append(B.concept(
    "First contact with any dataset: how big is it, what types came in, and where are the holes "
    "and duplicates? Never analyze before you've looked."))
p.append(B.code_example(
'''import pandas as pd

df = pd.read_csv("ecommerce_orders.csv")
print("shape:", df.shape)
print(df.dtypes)

# Where are the holes and duplicate rows?
print(df.isna().sum().loc[lambda s: s > 0])
print("duplicate rows:", df.duplicated().sum())''',
'''shape: (5015, 11)
order_id         int64
order_date      object      <- dates came in as text
customer_id      int64
region          object
category        object
channel         object
quantity         int64
unit_price     float64
discount       float64
order_value    float64
is_returned       bool
region    151                <- missing values
dtype: int64
duplicate rows: 15''',
    filename="01_load.py", runnable=False))
p.append(B.note(
    "Three red flags already: `order_date` is stored as text (we'll parse it), `region` has 151 "
    "missing values, and there are 15 duplicate rows. Spotting these now saves you from silently "
    "wrong answers later."))

# ---- step 2 clean ---- #
p.append(B.h2("Clean — make it trustworthy", kicker="Step 2"))
p.append(B.concept(
    "Cleaning is the unglamorous 80% of the job, and every decision is a judgment call you must be "
    "able to defend. Here we make four, and say *why* for each."))
p.append(B.code_example(
'''df = df.drop_duplicates().copy()                       # 1. exact dupes add nothing
df["order_date"] = pd.to_datetime(df["order_date"])    # 2. text -> real dates

# 3. Remove impossible order values (<= 0, or an absurd 99999 entry error)
bad = (df.order_value <= 0) | (df.order_value >= 10000)
df = df[~bad].copy()

df["region"] = df["region"].fillna("Unknown")          # 4. keep rows; label the gap
df["month"]  = df.order_date.dt.month
print(f"removed {int(bad.sum())} impossible order_value rows")
print(f"clean rows: {len(df)}")''',
'''removed 8 impossible order_value rows
clean rows: 4992''',
    filename="02_clean.py", runnable=False))
p.append(B.callout("warn", "Defend every cleaning choice",
    "We *dropped* exact duplicates and impossible values (they are errors, not signal), but we "
    "*kept* rows with a missing region and labelled them `Unknown` rather than deleting them &mdash; "
    "throwing away 151 real orders would bias the totals. Different choices change the answer, so a "
    "stakeholder is entitled to ask why you made each one. Have the reason ready.", "&#9650;"))

# ---- step 3 explore ---- #
p.append(B.h2("Explore — the headline numbers", kicker="Step 3"))
p.append(B.concept(
    "Now we aggregate (Lesson 4.2). Notice we report the **median** order value next to the mean, "
    "because order value is right-skewed &mdash; exactly the trap from the summary-statistics lesson."))
p.append(B.code_example(
'''gross = df.order_value.sum()
print(f"Gross revenue : ${gross:,.0f}")
print(f"AOV           : mean ${df.order_value.mean():.2f}  |  median ${df.order_value.median():.2f}")

bym = df.groupby("month")["order_value"].sum()
print(f"Q4 share      : {bym.loc[[10, 11, 12]].sum() / gross:.1%}")

by_cat = df.groupby("category")["order_value"].sum().sort_values(ascending=False)
print((by_cat / 1000).round(0).astype(int))   # revenue by category, $000s''',
'''Gross revenue : $802,017
AOV           : mean $160.66  |  median $94.08
Q4 share      : 38.5%
category
Electronics    399
Apparel        125
Home           118
Sports         113
Beauty          47''',
    filename="03_explore.py", runnable=False))

# ---- step 4 visualize ---- #
p.append(B.h2("Visualize — let the charts do the arguing", kicker="Step 4"))
p.append(B.concept(
    "A table of numbers rarely changes a mind; a clear picture does. Each chart below answers one "
    "question and carries one message in its title."))
p.append(B.figure(IMG + "cap_monthly.png",
    "**Revenue is highly seasonal.** Q4 (red) drives ~38% of the year, peaking in December. That "
    "single fact should shape inventory, staffing, and ad spend.",
    "Bar chart of monthly revenue with Q4 highlighted."))
p.append(B.figure(IMG + "cap_category.png",
    "**Electronics is the engine** at ~half of all revenue. Concentration like this is both a "
    "strength and a risk worth naming.",
    "Horizontal bar chart of revenue by category."))
p.append(B.figure(IMG + "cap_ordervalue.png",
    "**Order value is right-skewed**, so the mean ($161) sits well above the median ($94). Report "
    "the median as the 'typical' order &mdash; the exact lesson from 1.3, now on real data.",
    "Histogram of order value with mean and median lines."))
p.append(B.figure(IMG + "cap_returns.png",
    "**Apparel is a returns outlier** at ~18% versus ~10% overall. A high return rate silently "
    "eats margin, so this deserves a closer look.",
    "Bar chart of return rate by category with overall average line."))
p.append(B.figure(IMG + "cap_netimpact.png",
    "**The cost of those returns.** Comparing gross to net (returns excluded) revenue, Apparel "
    "loses the most ground &mdash; about $23k of its ~$125k gross never sticks.",
    "Grouped bar chart of gross vs net revenue by category."))

# ---- step 5 insight ---- #
p.append(B.h2("Insight and recommendation", kicker="Step 5 · the point of all this"))
p.append(B.concept(
    "An analysis that stops at charts is unfinished. The deliverable is a decision. Here is the "
    "whole story compressed to what a busy manager needs:"))
p.append(B.callout("why", "The one-paragraph insight a stakeholder can act on",
    "Revenue is **highly seasonal** &mdash; Q4 alone drives 38% of the year, so we should weight "
    "inventory, staffing, and ad budget toward the holidays, especially for **Electronics**, which "
    "is half of all revenue. The hidden problem is **Apparel**: its ~18% return rate (versus ~10% "
    "overall) quietly erases about \\$23k of net revenue. **Recommendation:** protect Q4 supply for "
    "Electronics, and launch a focused effort to cut Apparel returns (better sizing guides, clearer "
    "photos, fit reviews). A 3&ndash;4 point drop in Apparel returns recovers more net revenue than "
    "a comparable sales lift in a low-return category &mdash; a cheaper lever than chasing new "
    "demand.", "&#9678;"))
p.append(B.note(
    "Notice the recommendation is **specific, quantified, and actionable**, and it names the next "
    "step: a controlled experiment (Track 6) on a sizing-guide change would prove the return-rate "
    "effect *causally* rather than by correlation. That is how an analysis becomes a decision."))

p.append(B.keypoints([
    "A capstone is judged by its **decision**, not its charts &mdash; always end on an insight a "
    "stakeholder can act on.",
    "**Inspect before you analyze**: shape, dtypes, missingness, duplicates. Red flags found early "
    "are bugs avoided late.",
    "**Every cleaning choice is defensible or it's wrong** &mdash; drop errors, but keep real rows "
    "(label missing values rather than deleting them).",
    "Skewed money data &rarr; report the **median**; the mean overstates the 'typical' order "
    "(Lesson 4.2 in the wild).",
    "Give each chart **one message in its title**, and connect findings to a **next step** "
    "(here: an A/B test).",
]))

p.append(B.quiz([
    {"q": "The analysis labelled 151 missing-region rows as 'Unknown' instead of dropping them. "
          "Why is that usually the better choice here?",
     "options": [
        {"t": "Dropping 151 real orders would bias the totals; the orders are valid even though "
              "one field is missing",
         "correct": True,
         "why": "Right. Those orders have valid values everywhere else and contribute real "
                "revenue. Deleting them would understate totals and could skew category/channel "
                "breakdowns. Labelling keeps the information and makes the gap explicit."},
        {"t": "Because pandas cannot compute sums when any value is missing",
         "why": "pandas handles NaNs in most aggregations fine. The reason is about not throwing "
                "away valid revenue, not a tooling limitation."},
        {"t": "Because 'Unknown' is always more accurate than the true region",
         "why": "'Unknown' isn't more accurate &mdash; it just honestly marks missingness instead "
                "of discarding the row's real, non-missing data."},
        {"t": "It makes no difference either way",
         "why": "It can matter a lot: dropping ~3% of orders shifts totals and any region-based "
                "view, so the choice is a real, defensible decision."}]},
    {"q": "Why does the report quote the **median** order value (\\$94) rather than the mean "
          "(\\$161) as the 'typical' order?",
     "options": [
        {"t": "Order value is right-skewed, so a few large orders pull the mean above what most "
              "customers actually spend",
         "correct": True,
         "why": "Exactly the Lesson 4.2 point on real data. With a right skew the mean is inflated "
                "by the long tail; the median better reflects the typical order."},
        {"t": "The median is always larger than the mean, so it looks better",
         "why": "Here the median ($94) is *smaller* than the mean ($161). The choice is about "
                "honesty under skew, not which number looks bigger."},
        {"t": "The mean cannot be computed for money",
         "why": "The mean is perfectly computable; the issue is that it misrepresents the typical "
                "order when the distribution is skewed."},
        {"t": "Because the median ignores returns",
         "why": "Neither statistic is about returns here; the median is chosen because the order-"
                "value distribution is skewed."}]},
    {"q": "The recommendation ends by proposing an A/B test of a sizing-guide change. What does "
          "that add that this EDA cannot?",
     "options": [
        {"t": "Evidence of a causal effect — that the change actually *reduces* returns, not just "
              "that returns correlate with Apparel",
         "correct": True,
         "why": "Correct. EDA reveals patterns and associations; a randomized experiment is what "
                "lets you claim the intervention *caused* the improvement (the bridge to Tracks 6 "
                "and 8)."},
        {"t": "A prettier chart of the same numbers",
         "why": "The value isn't presentation &mdash; it's moving from correlation to a tested "
                "causal claim about the intervention."},
        {"t": "Nothing; the EDA already proves the sizing guide will work",
         "why": "EDA can't prove an intervention works; it shows Apparel returns are high, not "
                "that a specific fix lowers them. That requires an experiment."},
        {"t": "A larger dataset automatically",
         "why": "An A/B test is about randomized comparison for causal evidence, not merely more "
                "data."}]},
]))

p.append(B.practice([
    {"q": "Extend the analysis: compute **revenue per customer** and find what share of revenue "
          "comes from the top 10% of customers. Which Track-1 idea does the resulting distribution "
          "most resemble?",
     "sol": "Group by `customer_id` and sum `order_value`, then compare the top decile's total to "
            "the whole: `rev = df.groupby('customer_id').order_value.sum().sort_values()` and "
            "`rev.tail(int(0.1*len(rev))).sum() / rev.sum()`. You'll typically find a **heavily "
            "right-skewed** distribution where a small fraction of customers drives a large share "
            "of revenue &mdash; the same skew lesson from 1.3, and a reason to report the median "
            "customer value, not the mean."},
    {"q": "The marketing lead claims 'Mobile is taking over.' Sketch how you'd check that from "
          "this data, and name one trap to avoid.",
     "sol": "Group by month and channel and look at the **trend** of each channel's share over "
            "2025: `df.groupby(['month','channel']).order_value.sum().unstack()` then normalize "
            "each row to a share. A rising Mobile share would support the claim. **Trap:** a single "
            "year can't separate a real trend from seasonality or a one-off promo &mdash; and "
            "correlation with time isn't causation. State the uncertainty, and propose a longer "
            "window or an experiment before declaring a takeover."},
]))

p.append(B.deepdive(
    B.concept(
        "**The three honest ways to handle missing data.** When a value is absent you have three "
        "defensible moves, and the right one depends on *why* it's missing:") +
    B.concept(
        "1. **Drop** the row &mdash; only safe when missingness is rare and unrelated to what you're "
        "measuring, or the row is unusable. Risky because it can bias results if the missing rows "
        "differ systematically.\n"
        "2. **Impute** &mdash; fill with a sensible estimate (median for numbers, most-common for "
        "categories, or a model). Keeps sample size but invents data, so it can understate "
        "uncertainty.\n"
        "3. **Flag** &mdash; keep the row and mark the gap (our `Unknown`). Honest and "
        "non-destructive; ideal when 'missing' might itself be informative.") +
    B.concept(
        "We flagged because region was missing for only ~3% of orders and those orders are "
        "otherwise valid. We *dropped* the impossible `order_value` rows because a negative or "
        "99999 price isn't missing &mdash; it's an error with no recoverable truth. The discipline: "
        "match the remedy to the cause, and write down what you did so the analysis is reproducible "
        "and defensible."),
    title="Deep dive: drop, impute, or flag? Handling missing data honestly"))

p.append(B.callout("note", "Run it yourself, then defend it",
    "Download [the notebook](../notebooks/capstone_eda.ipynb) and [the dataset](../data/ecommerce_orders.csv), "
    "run every cell, and then say the insight paragraph out loud as if a manager were across the "
    "table. If you can explain each cleaning choice and connect the charts to the recommendation, "
    "you've passed the real test of this course: *could you run this analysis and defend it to a "
    "stakeholder?*", "&#9670;"))

LESSONS = {"cap-01-eda": "\n".join(p)}
